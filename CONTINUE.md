# CONTINUE.md — как продолжать проект (в т.ч. в новом чате)

> Цель файла: чтобы любой новый чат продолжал работу **по реальным фактам iiko, без выдумок**. Прочитай этот файл и перечисленные источники ПЕРЕД любыми правками.

## ГЛАВНОЕ ПРАВИЛО
**Ничего не выдумывать.** Каждая фича = реальный экран/поле iiko, подтверждённый документом. Источник истины — папка `iiko_spec/` (реверс iikoChain 2022 / iikoFront). Если фичи нет в спеке или есть сомнение — сверять по официальным источникам (для KZ: kgd.gov.kz, esf.gov.kz, ru.iiko.help, mybuh.kz), а не придумывать. Контекст — **Казахстан**: тенге ₸, ҚҚС 16%, БИН/ИИН, Webkassa, налоги ОПВ/ВОСМС/ИПН/СО.

## ИСТОЧНИКИ ИСТИНЫ (читать перед работой)
1. `iiko_spec/00_INDEX.md` — карта 16 модулей бэк-офиса + раздел кассы (FRONT_*). Файлы `01_…`–`16_…` = модули офиса; `FRONT_01/02/03` = касса.
2. `iiko_spec/<модуль>.md` + `iiko_spec/raw_docs/topic-*.md` — детальные поля/экраны по теме.
3. `iiko_spec/screens/` — реальные скриншоты (101 шт.) для сверки 1:1.
4. `iiko_spec/KZ_SPECIFICS.md` — казахстанские параметры (налоги/ЭСФ/формы).
5. `ARCHITECTURE.md` — что это, связь касса↔офис, стек, правило «офис настраивает — касса потребляет».
6. **`STATUS.md`** — единый срез: что сделано/не сделано (касса + офис). Начинать отсюда.
7. `PARITY_AUDIT.md` — детальный бэклог кассы (high закрыты, med/low — хвост).
8. `OFFICE_ROADMAP.md` — детальный план офиса по фазам.

## АРХИТЕКТУРА (как всё связано, без бэка)
Мок без сервера. Связка: **офис → `src/store/pos.ts` (zustand) → localStorage → касса (Front)**. Полный список ключей localStorage и где что видно на кассе — в `STATUS.md` (Часть C). Оперативные данные (смена/заказы) хранятся в ключе `iiko-runtime` и переживают перезагрузку. Точка будущей интеграции — заменить мок-функции в `src/store/pos.ts` + `src/mock/*` на fetch к .NET; экраны не трогать.

## РАБОЧИЙ ЦИКЛ КАЖДОЙ ПРАВКИ (паттерн)
1. Найти фичу в `iiko_spec/` → выписать реальные поля (не выдумывать).
2. Тип в `src/types.ts` (если нужен).
3. Стор `src/store/pos.ts`: state + действия + **персист в свой localStorage-ключ** (как у соседних сущностей).
4. Офис `src/screens/OfficeScreen.tsx`: CRUD/настройка (если фича офисная).
5. Касса: экран-консьюмер читает из стора (НЕ из статичных мок-массивов).
6. `npm run build` (tsc -b && vite build) — должно быть зелёно.
7. Обновить `STATUS.md` (перенести пункт в «сделано») и закоммитить.
Правило UI: тач-первый ввод (модалки-клавиатуры, не системные), **иконки lucide вместо эмодзи**.

## ЗАПУСК / ПРОВЕРКА
- `cd iiko-pos && npm run dev` → http://localhost:5180 ; сборка `npm run build`.
- PIN (мок): Петров 1111, Иванова 2222, Легасов 3333, Админ 0000.
- Граблю: `tsconfig.node.json` без `noEmit` (composite) — иначе TS6310.

## ЧТО ДАЛЬШЕ (см. STATUS.md «не сделано»)
Касса (med/low паритет): курсы подачи; дробный возврат qty; техкарты нетто/брутто + «выход блюда»; отдельные кнопки X/Z; отчёты 015/039; фильтры отчётов по залу/официанту; выбор склада в Warehouse; отправка чека email/SMS; вход списком сотрудников; контекст-меню стола.
Офис (OFFICE_ROADMAP): 1D типы заказов; Phase 4 прейскурант (ценовые категории/периоды); Phase 5 сотрудники (расписание/смены/медкнижки); Phase 6 администрирование (лицензии/устройства); Phase 7 финансы (кассовая книга/ДДС). Крупные: 14 Доставка, 15 iikoCard, 13 Журналы.

## ОРГ
- Репозиторий: github.com/AdilFromAstana/kassa, ветка `main`. Несколько коммитов сделаны локально и **не запушены** — при необходимости `git push` (через Bash; сеть).
- `gh` не установлен.
