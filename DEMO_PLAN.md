# DEMO_PLAN.md — доведение кассы до полноценного демо на моках (без бэка)

> Цель: показать **полноценно работающий пример кассы+офиса** на мок-данных — всё кликается, логика работает «как на реальных данных», persist в localStorage.
> Бэкендер подключится позже **без переписывания UI** (см. блок C — слой-репозиторий).
> Контекст: Казахстан (₸, ҚҚС 16%, БИН/ИИН, Webkassa/ОФД РК). Точка входа трека — `HANDOFF.md`.
> Статус трека «реально сделать» (1–5) — закрыт; этот документ = следующая фаза «демо-полнота».

## Главная идея
Всё уже идёт через `src/store/pos.ts` (zustand) + localStorage. Чтобы стало убедительным демо и легко приняло бэк, нужно 3 вещи: **богатые «живые» данные**, **замкнутые сквозные сценарии**, **тонкий слой, имитирующий бэк**.

---

## A. Сделать данные «живыми» (ПРИОРИТЕT №1 — максимум эффекта «как настоящее») — ✅ СДЕЛАНО
- ✅ **Демо ВКЛ по умолчанию** (`loadDemoAuto` → on, пока флаг `iiko-demo-auto` ≠ '0'); выключается в Доп.
- ✅ Расширен `src/mock/demo.ts`: ~52 закрытых заказа за рабочий день (~11ч), зал/вынос/доставка, разные официанты/оплаты/скидки, чаевые, питание персонала, **оплата частично бонусами** (карта гостя привязана); 5 открытых заказов с **кухонными статусами** (под KDS); внесения/изъятия; 5 актов списания.
- ✅ **Архив прошлой смены** (`ClosedShift` №107) — Z-архив/печать Z не пустые.
- ✅ Seed **приходных накладных** (`invoicesSeed`, 2 шт) → бухгалтерия/закупки/проводки живые.
- ✅ В сиде заведения включены **delivery + iikoCard** (видны плитки/вкладки на кассе).
- Эффект: OLAP-куб, отчёты, бухгалтерия/ДДС, мотивация, лояльность, доставка — сразу показывают осмысленные числа.
- Осталось из A (по желанию): кнопки «Сгенерировать день/Сбросить» вынесены в блок E.

## B. Замкнуть сквозные сценарии (e2e, кликается от начала до конца, с persist)
1. Смена: открытие → заказы (зал/вынос/доставка) → оплата (все типы) → фискальный чек → инкассация → Z-закрытие → отчёты отражают.
2. Склад: закупка (накладная) → продажа списывает по техкарте → инвентаризация → отклонения → проводки в бухгалтерии.
3. Лояльность: выпуск карты → начисление → списание/депозит/сертификат.
4. Доставка: приём → статусы 0–5 → курьер → закрытие.

## C. Эмуляция бэка во фронте (ПРИОРИТЕT №2 — точка будущего подключения бэка) — 🟡 В РАБОТЕ
- ✅ **Слой-репозиторий** `src/api/` (контракт async `PosRepository`, `localRepository`, `httpRepository`-заготовка, `index.ts` — одна точка переключения, `CONTRACT.md`). Проверен round-trip-смоком 6/6. Запись runtime уже идёт через `repo.saveRuntime`. Детали и статус C1–C4 — в `BACKEND_MIGRATION_PLAN.md`.
- ✅ C3 все записи конфиг-модулей через `repo.saveConfig` (33 точки, те же ключи); ⬜ C4 async-гидратация чтения (нужна только при переезде на http).
- **Симуляция реального времени**: таймеры KDS, авто-смена статусов доставки, тающий счётчик «непереданных в ОФД», presence сотрудников.
- **Мок-фискализация**: номера ФД, QR на чеке, очередь ОФД — выглядит как Webkassa.

## D. Полнота интерактива (хвост паритета — заметные фичи)
- KDS: статусы блюд готовится/готово/подано + таймер просрочки (флаг `kitchenScreen`).
- Мультиставка ҚҚС в чеке и отчётах (16%/0%).
- Экспорт в Excel отчётов/OLAP (фронт через blob — реально работает).
- Tab/барный счёт, контекст-меню стола, отправка чека email/SMS.
- Печать-превью везде (чек/пречек/накладная/Z) единым стилем.
- Прочее из `STATUS.md` A2 / B2 (частичная смена типа оплаты, стоп по залу, отчёты 015/039, Избранное, Помощь и т.д.).

## E. Демо-обвязка (для показа) — ✅ СДЕЛАНО (тур — опц.)
- ✅ **Демо-данные** (Доп. → Инструменты → «Демо-данные»): «Сгенерировать день» (`seedDemo`), «Очистить» (`clearDemo`), вкл/выкл авто-наполнение (`setDemoAuto`) + счётчик чеков.
- ✅ **Быстрый вход по ролям** (LoginScreen, режим PIN, при demoAuto) — кнопки сотрудников → сразу в меню, показать гейты прав.
- ✅ **Бейдж «DEMO · данные мок»** в углу экрана входа.
- ⬜ короткий тур по экранам — опционально, низкий приоритет.

## Рекомендуемый порядок
**A (живой день) → C (репозиторий + симуляции времени) → D (KDS/ҚҚС/Excel/печать) → E (обвязка).**

---

## ЧИСТО ФРОНТОВЫЙ БЭКЛОГ (зависит только от нас, всё мок-able)
> Только то, что реализуемо на фронте без бэка/железа. Слои вне нашего контроля — в разделе ниже, их не трогаем.

### Касса (iikoFront)
- [x] **KDS — кухонный экран** `/kitchen` (✅): билеты заказов по станциям Кухня/Бар, статусы Новое/Готовится/Готово, таймер ожидания + красная просрочка >15′, тап продвигает статус, «Всё готово»; флаг `kitchenScreen` ВКЛ в сиде
- [x] **Мультиставка ҚҚС** — ✅ в фиск. чеке, кассовых отчётах и офисе (отчёт «ҚҚС (НДС)» по ставкам 011/041, «Без НДС» по ставке позиции, P&L/налоги rate-aware; демо-доставка несёт строку-услугу 0%)
- [ ] **Tab / барный счёт** (`Order.tabName`, флаг `tab`) — именованный счёт без стола
- [ ] **Контекст-меню стола** (долгое нажатие: открыть/перенести/объединить/забронировать) + маркер брони на плитке зала
- [ ] **Частичная смена типа оплаты** (распределение суммы по типам, гейт `F_CHPAY`)
- [ ] **Печать Z из архива** закрытых смен (чек-превью с итогами/ҚҚС/типами оплат)
- [ ] **Авторизация `F_CRCT`** для чека коррекции (нет права → прокатка карты менеджера)
- [ ] **Отправка чека email/SMS** на экране закрытого чека (мок)
- [ ] **Единый стиль печати-превью** (чек/пречек/товарный/накладная/Z) — компонент-превью
- [ ] **Отчёты 015** (открытые заказы/продажи по залам) **/ 039** (вскрытия тары) + фильтры по залу/официанту над отчётом
- [x] **Excel-экспорт** отчётов/OLAP — ✅ кнопка «Excel…» (CSV+BOM, `lib/export.ts`) в офис-отчётах (продажи/остатки/ҚҚС) и в OLAP-сводной

### Офис
- [ ] **01 Избранное** — стартовая страница быстрого доступа
- [ ] **05 Быстрое меню** (прейскурант доп.)
- [ ] **06 Расписание / типы смен / явок** (частично есть)
- [ ] **10 Флаер / шаблоны карт** (дисконт доп.)
- [ ] **13 Монитор синхронизации** — UI-журнал (мок-лог)
- [ ] **16 Помощь** — справка

### Демо-обвязка и «оживление» (чисто фронт)
- [x] **Генератор «живого дня»** — `src/mock/demo.ts` расширен (✅, демо ВКЛ по умолчанию)
- [x] **Кнопки «Сгенерировать день» / «Очистить» + вкл/выкл авто** — ✅ Доп. → Инструменты → «Демо-данные»
- [x] **Быстрое переключение ролей** — ✅ быстрый вход по ролям на экране входа (demoAuto)
- [x] **Бейдж DEMO** — ✅ (тур — опц., не делаем)
- [ ] **Симуляции реального времени** (фронт): таймеры KDS, авто-смена статусов доставки, тающая «очередь ОФД», presence сотрудников
- [ ] **Слой-репозиторий `src/api/`** (промисы + искусственная задержка на localStorage) — фронтовая архитектура под будущую подмену на `fetch`

## Что упирается в .NET / внешние API (НЕ делается на фронте)
- Реальная фискализация Webkassa / ОФД РК (КГД).
- Доставка: геокодер/карта/GPS/тепловая карта (Google/2ГИС/iikoRadar).
- «Настоящие» OLAP-сервер на больших данных, проводки бухгалтерии в БД, POS-сервер iikoCard5, интеграция устройств (ФР/сканер/весы/дисплей), многотерминальная синхронизация в реальном времени.

## Важно про охват (не вводить в заблуждение)
Это **мок-демо ключевых модулей** iiko (касса iikoFront + офис iikoChain/iikoOffice 01–16), с матрицей прав (F_*/B_*/D_*, 8 ролей) и сквозной логикой на localStorage — **не полное 1:1 воспроизведение реальной айки**. Глубина реальных сотен отчётов/настроек, серверная логика, фискализация и устройства — упрощены или отсутствуют (см. выше). Демонстрируются основные бизнес-флоу и ролевое разграничение, а не вся функциональность продукта.
