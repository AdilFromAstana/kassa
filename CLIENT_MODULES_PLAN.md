# CLIENT_MODULES_PLAN.md — модули «от боли реальных клиентов»

> Срез 2026-06-21. Источник: запрос Адиля (12 пунктов от реальных клиентов настоящей iiko).
> Суть: всё ниже — это **неудобства или нехватка функционала в НАСТОЯЩЕЙ iiko**, которые клиенты просят закрыть лучше.
> Легенда статуса основы: ✅ есть движок · 🟡 есть данные, нет витрины · ⬜ нет.
> Контекст РК: тенге ₸, ҚҚС 16%, мульти-точка/мульти-сеть (бэк уже это умеет).
>
> **Правило работы (по просьбе Адиля): перед КАЖДЫМ этапом — детальный план, потом реализация + сборка/тест.**

## ПРОГРЕСС
- ✅ **Этап 1 — роли** (2026-06-21): +Технолог, +Собственник (Бухгалтер уже был). `rights.ts` + `Rights.cs`. Сборки зелёные.
- ✅ **Этап 2 — low-stock уведомление** (2026-06-21): `lib/stockAlerts.ts` (селектор) + бейдж-попап в `TopBar.tsx` + WarehouseScreen на селекторе. Тест 5/5, build зелёный.
- ✅ **Этап 3 — витрины 4/5/7** (2026-06-21): `lib/salesReports.ts` (ordersByWaiter/bestWaiter/topDishesByDay) + тест 5/5. Офис→Отчёты новая вкладка «Топ-5 блюд по дням» (матрица день×блюдо) + «🏆 Лучший официант» в разрезе «По официантам» (модуль 5 по сути уже был = revByWaiter). Attendance: сводка Пришло/Опозданий/Прогулов. Смоук покрывает новую вкладку. Build + юнит 67/67 зелёные.
- ✅ **Этап 4 — модуль 6 (% официанта на закрытии)** (2026-06-21): селектор `waiterCommission` в salesReports.ts (+тест), поле `Establishment.waiterCommissionPct` (дефолт 3) + инпут в офис-настройках, панель «Расчёт официантов (N%)» в мастере закрытия смены (шаг 4: официант·заказов·выручка·к выплате·итог). Build + юнит 69/69.
- ✅ **Этап 5 — модуль 8 (проходимость/гости)** (2026-06-21): селектор `footfall` (total/byDay/byHour/peakHour) + `hourKey` в salesReports.ts (+3 теста); офис→Отчёты новая вкладка «Проходимость (гости)» (По дням/По часам, итог гостей, пиковый час 🔥). Гости и так захватывались везде; добавлена именная витрина + разрез по часам. Build + юнит 72/72.
- ✅ **Этап 6 — модуль 12 опен-меню (доп-ингредиенты)** (2026-06-21): СПИСАНИЕ модификаторов со склада уже работало (`consumptionDelta`+`modifierTechCards`). Дыра была в СЕБЕСЕ — закрыта: `modifierCost`/`orderLineCost` в warehouse.ts (доп мясо поднимает себес), OfficeScreen `lineCost(l)` modifier-aware → COGS/P&L/salesByDish корректны; WarehouseScreen вкладка техкарт — секция «Доп-ингредиенты модификаторов». Тест warehouse.test.ts (6). Build + юнит 78/78. BACKLOG: CRUD-редактор техкарт модификаторов (сейчас статический сид).
- ✅ **Этап 7 — модуль 9–11 перемещения** (2026-06-21): рефактор склада на per-store (единый источник `storeStock`, `i.stock`=производная сумма; см. `TRANSFER_PLAN.md`). Починен скрытый баг (перемещение «теряло» товар). Сделано: п.10 (склад↔склад) + п.11 (с кассы, право `B_TRN` Менеджеру/Технологу/Управляющему). Сеть безопасности Ш0 цела. Build+бэк+юнит 87/87. ⛔ п.9 (мультиресторан/точка↔точка) = .NET backlog.
- ✅ **Визуальный QA (2026-06-21, Playwright)** — прокликаны и подтверждены скриншотами все модули: роли (10 ролей, колонка Технолог), low-stock (попап «Лосось закончился / Сливки 0,5») , топ-5 блюд (матрица день×день), проходимость (дни/часы, пик 18:00), расчёт официантов (3%, математика сходится), лучший официант (🏆), перемещения (per-store склад «Бар» + форма источник→приёмник с остатком). **Найден+починен баг:** индикатор low-stock не показывался на ГЛАВНОМ меню (меню использует свой хедер, не общий TopBar) → добавил индикатор в `MainMenuScreen` хедер. Build+юнит 87/87.
- ⏭️ **Дальше:** B Telegram-бот (XL, требует живой .NET + webhook). Селекторы (lowStock/ordersByWaiter/topDishesByDay/footfall/waiterCommission) готовы как источники.
- Прогоны: юнит 87/87; 2 live-БД-теста (`auth.integration`) красные из-за окружения (Postgres :5432 занят SSH-туннелем, контейнер iikopos-db потушен) — НЕ связано с правками.
- Коммитов пока НЕТ (ждём явной просьбы Адиля). Репо фронт: github.com/AdilFromAstana/kassa; бэк отдельной папкой.

## Что уже есть в проекте (опора)
- **Фронт** (`iiko-pos`, React+Vite+TS, мок localStorage): касса iikoFront + бэк-офис iikoOffice 01–16.
- **Бэк** (`iiko-pos-backend`, .NET 10 + Postgres, ~88 таблиц): Платформа→Tenant(сеть)→Юрлицо→Подразделение→ТП(точка)→Склад. Auth/RBAC, склад receive/inventory, касса, закупки+бухгалтерия, отчёты. Фронт пока ходит в бэк только через compat (`config`/`runtime`), доменные экраны НЕ подключены.
- **Матрица прав**: `src/lib/rights.ts` / `Domain/Rights.cs` (~80 кодов, 8 ролей).
- **Attendance** (явки), **зарплата** (повременка из явок + премия от продаж), **OLAP-куб**, **stockCrit** (критический остаток — данные есть, алерта нет).

---

## 12 модулей — разбор

### A. Роли и доступы
**1. Роли: технолог / собственник / бухгалтер** — ✅ **СДЕЛАНО 2026-06-21** (Бухгалтер уже был; добавлены Технолог и Собственник в `rights.ts` + `Rights.cs`, обе сборки зелёные). Исходный замысел — пресеты ролей поверх матрицы:
- *Технолог* — техкарты/номенклатура/себестоимость/потери; без денег/кассы.
- *Бухгалтер* — финансы/проводки/ЭСФ/отчёты/опен-меню (см. п.12); без правки меню.
- *Собственник* — read-all + отчёты/OLAP по всей сети.
- Боль iiko: роли размыты или требуют дорогой лицензии/тонкой ручной настройки.
- Тип: **неудобство** (пресеты поверх готового движка).

### B. Уведомления (гибкий хаб + Telegram-бот) — уточнено Адилем
Не просто алерты, а **гибкая система уведомлений и отчётов**, главный канал — **Telegram**.

**2. Уведомление «закончились товары»** — ✅ **СДЕЛАНО 2026-06-21**. `src/lib/stockAlerts.ts` (селектор out=закончился ≤0 / low=заканчивается ≤min, единый для склада/TopBar/бота) + индикатор-бейдж с попапом в `TopBar.tsx` (виден на всех экранах кассы, реактивен) + WarehouseScreen переведён на тот же селектор. Юнит-тест `stockAlerts.test.ts` (5), сборка зелёная. Селектор готов как источник для Telegram-хаба (этап B).

**3. Push / Telegram-уведомления + бот-отчёты** — ⬜ нет канала доставки. Состав:
- **Telegram как основной канал** доставки уведомлений бастыку (руководителю/собственнику).
- **Авто-уведомление ПОСЛЕ ЗАКРЫТИЯ СМЕНЫ (обязательно):** в сообщение падает **базовая инфа о сегодняшней смене** — выручка, чеки/средний чек, наличные/безнал, и т.п.
- **Интерактивный бот (кнопки):** в Telegram кнопки по блокам отчётов — бастык нажимает нужный блок, бот **отвечает** соответствующим отчётом. Блоки (расширяемые):
  - приход/уход сотрудников,
  - лучший официант,
  - топ-5 блюд,
  - выручка/смена,
  - и прочие новые (список наращиваемый).
- **Flexible / конфигурируемость:** какие события шлём, кому (получатели = бастыки/собственники), какие блоки доступны в боте, расписание авто-отчётов.
- Связь с другими пунктами: тянет данные из п.4 (приход/уход), п.5 (заказы/официант), п.6 (% официанта), п.7 (топ блюд), п.8 (гости). По сути это **витрина-доставка поверх тех же отчётов** + on-demand через бота.
- Тип: **нехватка функционала** (новый: транспорт Telegram-бот + движок отчётов по запросу + авто-сводка на закрытии смены).

### C. Сотрудники / официанты
**4. Приход времени сотрудников** — 🟡 Attendance уже считает приход/уход/план/факт/опоздание; нужна нормальная **витрина/табель/выгрузка**. → **неудобство**.
**5. Количество заказов на официанта** — 🟡 OLAP тянет (измерение «Официант»), нет простого показателя «N заказов/официант за день». → **неудобство**.
**6. Официант закрыл кассу 50к → сразу % (3%) за сегодня** — ⬜ премия от продаж есть, но НЕ мгновенно на закрытии и НЕ как % с ЛИЧНОЙ выручки официанта. Нужно: на закрытии смены/дня по официанту считать его выручку и тут же `комиссия = выручка × ставка%` (ставка из профиля/настройки). → **нехватка** (новая логика на закрытии).

### D. Аналитика
**7. Топ по блюдам за неделю по дням** — 🟡 OLAP может, нужен готовый **отчёт-витрина** день×блюдо за неделю. → **неудобство**.
**8. Сколько человек пришло в ресторан (проходимость)** — ⬜ нет поля «кол-во гостей» как метрики и нет отчёта footfall. Нужно: поле guests в заказе + отчёт. → **нехватка**.

### E. Склад и перемещения (крупнейший блок)
**9. Один склад на несколько ресторанов (центральный → раздаёт мясо по точкам)**
**10. Перемещение: выбор продукта, отпускаю — видят «бастыктар» (руководство) все**
**11. Перемещение делать с АДМИНА (кассы), а не только в бэк-офисе**
→ всё про **документ «Перемещение» (Transfer)** между складами/точками сети. Сейчас есть приход/инвентаризация/списание, но **Перемещения нет**. Бэк уже мульти-точка/мульти-склад — фундамент есть.
- центральный склад → расход + приход на точке;
- перемещение точка↔точка;
- **Кто делает / кто видит (уточнено Адилем):** перемещение проводят **сами менеджеры точек** (выбор продукта, отпуск с одной точки → приём на другой). **Админы/руководство просто ВИДЯТ факт операции** (что перемещение было), сами его не создают. → у менеджера = право создавать Transfer; у админа = read-only обзор всех перемещений по сети.
- **доступ из терминала-кассы (админ-режим)**, не только офис.
- Тип: **нехватка функционала + неудобство расположения** (в реальной айке перемещение живёт только в офисе).

### F. Опен-меню (доп-ингредиенты) — ГЛАВНЫЙ по сложности
**12. «Опен меню» = модификаторы с реальным доп-расходом ингредиента.**
Кейс: блюдо + юзер нажал «доп мясо». В НАСТОЯЩЕЙ iiko это часто НЕ учитывается → потрачено больше мяса, чем списано → недостача + заниженная себестоимость.

**Как реально работает в iiko (research):**
- Модификатор **с техкартой** → ингредиенты списываются со склада и входят в себестоимость (платные допы).
- Модификатор **без техкарты** → только комментарий («без льда»), на склад не влияет.
- **Почему боль / почему многие НЕ включают:** на каждый модификатор нужна своя техкарта + тип приготовления + склад списания → много ручной настройки; частые ошибки (не та фасовка/склад/ТТК) → расход не сходится. Поэтому «доп мясо» пробивают как бесплатный комментарий → реальный расход > учётного.

**Что нужно сделать у нас (лучше, чем в iiko):**
- Модификатор-доп несёт **дельту ингредиента** (напр. +80 г мяса) с минимумом настройки.
- При оплате: списать ингредиент модификатора со склада ДОПОЛНИТЕЛЬНО к технике блюда.
- Поднять **себестоимость строки заказа** на стоимость допа.
- Отразить в отчётах/OLAP (себес/прибыль rate-aware уже есть).
- Тип: **нехватка функционала** (учёт доп-ингредиента «из коробки», без боли настройки).

Sources (research опен-меню/модификаторы):
- https://open-s.info/blog/nastroyka-modifikatorov/
- https://open-s.info/blog/sebestoimost_polufabrikati_iiko/
- https://open-s.info/blog/kak_pravilno_spisyvat_produkty_i_blyuda_v_iiko/
- https://ru.iiko.help/resources/Storage/archive/PDF/RU_iikoOffice_6.0.pdf

---

## Сводка по типам
| Тип | Пункты |
|---|---|
| Неудобство (данные/движок есть, нужна витрина/пресет) | 1, 4, 5, 7 |
| Нехватка функционала (новый код) | 2, 3, 6, 8, 9–11 (перемещения), 12 (опен-меню) |

## ОЦЕНКА ОБЪЁМА (срез 2026-06-21, по факту кода)

### Что УЖЕ есть (опора, переиспользуем)
- Матрица прав 8 ролей/~80 кодов (фронт `rights.ts` + бэк `Rights.cs`/`RoleRight`/`RightDef`).
- Attendance (приход/уход/план/факт/опоздание), motivationPrograms (премия за продажи), PayProfile.
- OLAP-куб (измерение Официант/Блюдо/День уже есть) — топ-блюд и заказы/официант там СЧИТАЮТСЯ.
- `stockCrit` (критич. остаток — порог есть, алерта нет).
- Бэк: `ModifierTechCardItem` (техкарта модификатора!), `StockBalance` по точке, отчёты sales/extra/balance/pnl.

### Чего НЕТ (надо писать)
- Документа **«Перемещение»** между складами/точками (TransferModal = перенос заказа на стол, не путать).
- На фронте остатки **НЕ разбиты по складам** (`stockMoves.ts`: «один склад на ингредиент — мок-упрощение») → для реальных перемещений нужна модель per-store.
- Модификаторы на фронте склад **не трогают** (`{id,name,price}`, без техкарты/дельты).
- Любого транспорта уведомлений (push/Telegram), движка отчётов-по-запросу, авто-сводки на закрытии.

### Что НЕ надо делать
- **Не строить отдельный web/native push** — «пуш» (п.3) закрываем через **Telegram-бот** (для бастыка это и есть пуш). Экономия большого куска инфры.
- Витрины п.4/5/7 **не писать с нуля** — переиспользовать OLAP/closedOrders, обернуть в готовый отчёт + блок бота.
- Не трогать `TransferModal` (это про столы).

### Оценка по модулям (T-shirt + ~дни, solo+AI; «реально», т.е. фронт+бэк)
| # | Модуль | Размер | ~дни | Фронт | Бэк | Заметка |
|---|---|---|---|---|---|---|
| 1 | Пресеты ролей технолог/собственник/бухгалтер | S | 1 | да | да | в основном конфиг поверх матрицы |
| 2 | Low-stock алерт | S | 0.5–1 | да | да | порог есть, нужен генератор события |
| 4 | Витрина прихода сотрудников | S | 1 | да | м.б. | данные есть, нужна витрина/выгрузка |
| 5 | Кол-во заказов/официант | S | 0.5–1 | да | да | витрина над OLAP/closedOrders |
| 7 | Топ-5 блюд неделя×день | S–M | 1 | да | да | витрина над OLAP |
| 6 | % официанта на закрытии (3% с личной выручки) | M | 2 | да | да | новая логика на закрытии + в ЗП |
| 8 | Счётчик гостей/проходимость | M | 1.5–2 | да | да | поле guests в заказе + отчёт |
| 12 | Опен-меню (доп-ингредиент списывается+себес) | L | 3–5 | да(много) | да(частично есть) | фронт с нуля; бэк есть ModifierTechCardItem |
| 9–11 | Перемещения склад/точки + доступ с кассы (менеджер создаёт, админ видит) | L–XL | 5–8 | да(много) | да | подвох: фронт-остатки не per-store |
| B/3 | Хаб уведомлений + **Telegram-бот** (авто-сводка на закрытии + кнопки-отчёты) | **XL** | 8–12 | да | да(много) | требует ЖИВОЙ бэк + публичный webhook/бот-токен; самый крупный/рисковый |

### Итого и зависимости
- **Грубо: ~6–9 недель solo+AI на всё «по-настоящему».** Если мок-фронт без интеграции — заметно меньше, НО Telegram и перемещения всё равно требуют рабочего .NET-бэка.
- **Стратегический блокер:** сейчас фронт ходит в бэк только через compat (`config`/`runtime`). Telegram-бот, перемещения, % официанта «по правде» хотят, чтобы доменные экраны были подключены к API. То есть до этих модулей логично сделать **подключение фронта к доменным эндпоинтам** (см. backend HANDOFF п.6).
- **Дешёвые победы (≈4 дня вместе): 1, 4, 5, 7** — пресеты ролей + три витрины. Хорошо стартовать с них (заодно готовят контент для бота).
- **Средние: 2, 6, 8.** **Крупные: 12 (опен-меню), 9–11 (перемещения), B (Telegram-бот).**
- Рекомендованный порядок: (1,4,5,7) → 2 → 6/8 → 12 → 9–11 → Telegram-бот последним (он агрегирует всё, что сделали).

## Заметки к реализации
- Подтверждённые Адилем интерпретации: п.1–11 — «всё правильно». п.12 уточнён (доп-ингредиенты).
- Цикл правки фронта: тип `src/types.ts` → стор `src/store/pos.ts` (+ persist в свой ключ) → консьюмер → `npm run build` → STATUS/ROADMAP → коммит (по одной фиче).
- Перемещения и опен-меню затрагивают и бэк (.NET): новые сущности Transfer + ModifierRecipe/ingredient-delta.
