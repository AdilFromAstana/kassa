# HANDOFF.md — точка передачи (продолжить в новом чате)

> Прочитай ЭТОТ файл первым, затем `DEMO_PLAN.md`, `CONTINUE.md`, `STATUS.md`, `OFFICE_ROADMAP.md`, `PARITY_AUDIT.md`.
> **СЛЕДУЮЩАЯ РАБОТА (новый чат): сквозной QA-прогон по `QA_SCENARIOS.md`** — прокликать реальные бизнес-кейсы (касса+офис+связки+роли+динамика), баги писать в «Дефекты» в конце файла, чинить. До этого делалась только сборка+boot, ручного QA не было.
> Цель текущего этапа: **довести «MVP-области» до «реально сделано»** (не заглушки), по одной, с коммитами.
> **Трек «реально сделать» (1–5) закрыт.** Идёт фаза `DEMO_PLAN.md` (демо на моках): **A «живые данные» — ✅ сделано** (демо ВКЛ по умолчанию, полный рабочий день + архив прошлой смены + приходные + delivery/iikoCard в сиде). D: **мультиставка ҚҚС + Excel-экспорт + KDS (`/kitchen`) — ✅**. E демо-обвязка: **генерация/очистка дня + быстрый вход по ролям + бейдж DEMO — ✅** (тур — опц., пропущен). Печать-превью единым стилем — решили НЕ делать (низкая ценность). C (слой-репозиторий под бэк) — 🟡 начат: `src/api/` (контракт+localRepository+httpRepository-заготовка+`CONTRACT.md`), запись runtime через `repo.saveRuntime`, проверен round-trip-смоком 6/6. План/статус C1–C4 — `BACKEND_MIGRATION_PLAN.md`. C3 ✅ (все конфиг-записи через `repo.saveConfig`, 33 точки). Точечные A2-фичи (Tab, контекст-меню стола, частичная смена оплаты, товарный чек, отправка чека) — оказались уже реализованы; STATUS A2 «Заказ/оплата/зал» приведён в соответствие. **`window.prompt` устранён везде** → единый `components/InputModal` (тач-модалки: Tab, комментарий, приход склада, удержание/штраф, отмена доставки). Осталось: C4 (async-гидратация чтения — только при переезде на http).
> Контекст — Казахстан: тенге ₸, ҚҚС 16%, БИН/ИИН, Webkassa/ОФД РК, налоги РК.

## Где мы сейчас (на момент передачи)
Функционально реализованы **все бизнес-модули** касса (iikoFront) + офис (iikoChain/iikoOffice 01–16, кроме мелочей). Это мок на localStorage (без .NET/реальной фискализации/устройств). Архитектура: офис → `src/store/pos.ts` (zustand) → localStorage → касса. Крупные офисные разделы вынесены в отдельные компоненты: `OfficeDelivery.tsx`, `OfficeCorp.tsx`, `OfficeLedger.tsx`.

## Запущенный трек «реально сделать» (НЕ MVP) — статус
Пользователь попросил довести до полноты 6 областей. Делаем по одной, глубоко, с коммитами:
1. ✅ **Права: полная матрица F_*/B_*/D_*** — сделано. `src/lib/rights.ts` (полный каталог ~80 кодов по `iiko_spec/PERMISSIONS.md`, 8 ролей, группы). Офис «Сотрудники → Права» = матрица роль×право (группы/поиск/скоуп/«выдать группу»). Тонкая блокировка кнопок: OrderScreen (F_MPR/F_COW), FiscalCommands (F_XR/F_ZREP), PersonalPage ЗП (F_VPSD), Banquets (F_ERS), OfficeDelivery (D_* + маска телефона D_SFN). Базово гейтятся ещё F_OCS/F_CS/F_CASH/F_STRN/F_SWWOFF/F_ID/F_CHPAY/F_CRCT/F_EM/F_CVS/F_VRPT.
2. ✅ **Бухгалтерия: двойная запись** — сделано. `src/screens/OfficeLedger.tsx` (Финансы → вкладка «Бухучёт»): план счетов РК (`chartOfAccountsSeed`/`openingBalancesSeed` в data.ts), авто-проводки от продаж/закупок/ЗП/кассы + ручные (`manualPostings` в сторе, persist `iiko-postings`), ОСВ, баланс. Баланс сходится по построению (каждая проводка сбалансирована, начальные сальдо равны).
3. ✅ **Настоящий OLAP-куб** — сделано. `src/screens/OfficeOlap.tsx` (Отчёты → вкладка «OLAP-отчёт»): сводная таблица из `closedOrders` (грейн = строка чека). DnD-измерения (День/Час/Официант/Тип заказа/Тип оплаты/Блюдо/Категория/Зал) по зонам Строки/Колонки/Фильтры/Доступные; «Данные» — мультипоказатели (выручка/чеки=distinct orders/кол-во/средний чек/себест по техкартам/прибыль без ҚҚС); вложенные заголовки колонок со spans + грандтотал-колонка «Итого»; разворачивание/сворачивание иерархии строк со субитогами; фильтры по значениям (чекбоксы); сохранённые отчёты (persist `iiko-olap-reports`). Многооплатные чеки относятся к основному (max) типу оплаты — суммы не задваиваются. OLAP-lite (`olapDim`/`olapMeasure`) удалён.
4. ✅ **iikoCard: депозит/сертификаты/конструктор акций** — сделано. `src/screens/OfficeLoyalty.tsx` (Офис → iikoCard, 4 вкладки) + `src/lib/loyalty.ts` (helpers). **Конструктор акций** «условие→действие»: accruePercent (%-начисление, опц. маска категории + порог minSum), accrueFixed (фикс), paymentLimit (лимит оплаты бонусами %), welcome (приветственный) — правят бонусы реально (`promoActions`, persist `iiko-promo-actions`). **Депозит-кошелёк** (Денежная программа): `LoyaltyCard.deposit`, пополнение в офисе (`topUpDeposit`), оплата с кошелька на кассе; `depositProgram` (`iiko-deposit-program`). **Сертификаты**: выпуск (номинал/срок/статус), погашение на кассе (`certificates`, `iiko-certificates`). Касса (`PaymentScreen.tsx`): синтетические вкладки оплаты «Депозит»/«Сертификат» + рефактор бонусов на `redeemLimitPctOf`/`accrualFor`. `LoyaltyProgram` урезан до `{active}` (скаляры вынесены в акции). Гейт всех вкладок — `establishment.iikoCard`.
5. ✅ **Структура корпорации: редактируемый CRUD + накладные + инвентаризация** — сделано. (а) `OfficeCorp.tsx` дерево теперь редактируемое: тип `CorpTree` (ids на узлах), стор `corpTree`+`setCorpTree` (persist `iiko-corp-tree`), inline-CRUD на каждом уровне (юрлицо/подразделение/ТП/склад: добавить «＋» при наведении, удалить-корзина, имена правятся на месте, БИН у юрлица). CRUD считается в компоненте (clone→mutate→setCorpTree). (б) **Полная приходная накладная** (OfficeScreen accounting): шапка поставщик/склад-получатель/дата/№/ЭСФ + табличная часть со столбцами сумма+ҚҚС 16% по строкам + итоги; `addPurchase(supplierId, lines, header?)`, `Invoice.store`. (в) **3-шаговый мастер инвентаризации** (`DocumentsScreen.tsx`, тип «Инвентаризация»): шаг 1 склад+выбор позиций (выбрать/снять все), шаг 2 ввод факт. остатков, шаг 3 сверка (учётный/факт/откл./откл.₸ + итог) → провести = остатки выставляются равными факту (через `createStoreDoc('Инвентаризация')`).
6. 🟡 **Доставка: картография/геокодер/SMS/heatmap** — только мок-уровень: зоны как полигоны-данные + SMS-шаблоны (симуляция) реально; реальный геокодер/карта/GPS невозможны без внешних API (Google/2ГИС/iikoRadar) — это ограничение инфраструктуры, не кода.

## Что лучше делать дальше БЕЗ бэка (приоритет)
- **#5 (рекомендуется) — корпорация CRUD + накладные + 3-шаг. инвентаризация.** Чистый фронт+localStorage, большой связный кусок офиса, ничего внешнего не нужно. Лучший следующий шаг.
- **Хвост паритета кассы** (см. `STATUS.md` ЧАСТЬ A · A2) — тоже мок-able и быстрые победы: товарный чек (состав+реквизиты), фикс-сумма/скидка на позицию, KDS-статусы блюд, мультиставка ҚҚС в отчётах/чеке, отчёты 015/039, Excel-экспорт, контекст-меню стола.
- **НЕ сейчас (требует .NET/инфры):** реальная фискализация Webkassa/ОФД РК, #6 геокодер/карта/GPS, OLAP-сервер на больших данных, интеграция устройств (ФР/сканер/весы). OLAP/бухгалтерия/лояльность уже работают на моке — апгрейд до «настоящего» = backend-задача.

## Рефактор-проход (безопасная модуляризация, build-green по шагам)
- **P1 ✅** `components/Tabs.tsx` — единый таб-бар; применён во всех 7 таб-барах (OfficeScreen reports/finance/admin + OfficeCorp/Ledger/Loyalty/Delivery). Дублей `border-b-2` в экранах нет.
- **P2 🟡** `components/Modal.tsx` создан (overlay `bg-black/60` + светлая карточка, props width/className/z); `InputModal` переведён на него; адаптирован `DocumentsScreen` (viewDoc) как образец. Дальнейшая адаптация остальных светлых модалок — ОПЦИОНАЛЬНО и по одному (вложенный JSX легко разбалансировать — конвертировать аккуратно, build после каждой). Тёмные кассовые модалки (numpad/клавиатура `TextInputModal`) не трогать.
- **P3 🟡** дробление `OfficeScreen.tsx` (1719 → 1545). Метод: сперва карта зависимостей секции, потом вынос (компонент сам зовёт `usePos()`, свой локальный стейт). Вынесено: `OfficeJournal`, `OfficeAdmin` (+ ранее Ledger/Olap/Loyalty/Delivery/Corp). 
  - **Безопасные к выносу далее** (зависят только от `pos` + свой локальный стейт): `settings` (establishment), `discount` (discounts/clubCards + newDisc/newCard), `retail` (payment/cashop/writeoff/orderTypes), `menu` (prices/categories/techcards), `prikazy` (priceOrders + poLines/CalendarModal), `stock` (ingredients + stockCrit; модалку `recvItem` перенести в компонент).
  - **Завязаны на общие вычисления (НЕ трогать без переноса локалей, риск)**: `reports` (salesByDish/vatRowsKz/exportExcel/…), `accounting` (revenue/vatKz/purchase-форма/deductStaff-модалка), `staff`+`payroll` (okladOf/kzTax/motivationOf/hoursOf/матрица прав).
  - При выносе чистить неиспользуемые деструктуры из `usePos` (для admin/journal — уже почищено). Проверка: `grep -c '\bимя\b'` = 1 → только в деструктуре → удалять.
- **P4 (опц., риск средний)** слайсы `store/pos.ts`.

## Рабочие правила (соблюдать строго)
- **Ничего не выдумывать**: каждая фича = реальный экран/поле iiko из `iiko_spec/` (topic-*, `screens/`). Сомнение → сверять по KZ-источникам.
- Цикл правки: тип в `src/types.ts` → стор `src/store/pos.ts` (state + действие + **persist в свой localStorage-ключ**) → офис/касса-консьюмер → `cd iiko-pos && npm run build` (зелёный, `tsc -b && vite build`) → обновить `STATUS.md`/`OFFICE_ROADMAP.md` → **коммит** (по одной фиче).
- UI: тач-модалки, иконки lucide (не эмодзи). Большие разделы офиса — отдельным компонентом.
- Запуск: `cd iiko-pos && npm run dev` → http://localhost:5180. PIN: Петров 1111, Иванова 2222, Легасов 3333, Админ 0000.
- Грабля: `tsconfig.node.json` без `noEmit` (composite), иначе TS6310.

## Git
- Репо: github.com/AdilFromAstana/kassa, ветка `main`. **Много коммитов этой серии НЕ запушены** — пушить только по явной просьбе пользователя (через Bash; сеть). `gh` не установлен.

## Как продолжить (новый чат)
Трек 1–5 закрыт. Осталось: №6 (доставка-картография — ограничено инфраструктурой, нужны внешние API) и **хвост паритета кассы без бэка** (см. `STATUS.md` ЧАСТЬ A · A2: товарный чек, скидка на позицию, KDS-статусы, мультиставка ҚҚС, отчёты 015/039, Excel-экспорт, контекст-меню стола). Скажи ассистенту: «прочитай iiko-pos/HANDOFF.md и продолжай — возьми <пункт из A2>».
